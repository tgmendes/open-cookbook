services:
  postgres:
    image: postgres:17-alpine
    container_name: cookbook-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: cookbook_dev
    ports:
      - "5434:5432"
    volumes:
      - cookbook_pgdata:/var/lib/postgresql/data

volumes:
  cookbook_pgdata:
